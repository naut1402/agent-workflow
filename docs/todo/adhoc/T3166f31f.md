# Todo — T3166f31f

- **Issue / epic:** adhoc (task tạo từ prompt, chưa có issue)
- **Loại nợ:** other
- **Branch / PR tạo nợ:** `dev/1.1.5/T3166f31f_mode-toggle-via-settings`
- **Ngày tạo:** 2026-09-10

### Vì sao hoãn

Hai mục `[imo]` của review commit `748d497` nằm ngoài phạm vi "bật/tắt mode qua Setting": một là nợ cũ của cả nhóm settings, một là thay đổi UX chạm 2 feature khác. Sửa trong lượt này làm PR phình và trộn concern (`git-pr.md` §6.2).

### Việc cần làm khi đối ứng

- [ ] `SettingsBusiness` (`src/features/settings/business/index.ts`) là **code chết** — controller đi qua namespace import (`settingsBusiness.loadModesConfig()`), class không được tham chiếu ở đâu trong `src/` hay `tests/`. Lượt này thêm `getModesConfig()`/`saveModesConfig()` cho đồng nhất với 7 nhóm settings khác, tức nhân bản thêm code chết. Chốt: xoá cả class, hay cho controller đi qua nó.
- [ ] `navigateToMode(<mode đã tắt>)` là **no-op im lặng** (`src/App.vue` — `setMode` return sớm). Đúng chủ đích (E8/TC-B3), nhưng `AgentNlWizard.vue` và `ArtifactPanel.vue` dùng lối này ở đúng nhánh "đá người dùng sang mode khác để hoàn tất việc" → nút không phản hồi là dead-end UX. Hướng rẻ: cho `setMode` trả `boolean` để 2 call site tự disable nút hoặc hiện hint "mode đang tắt trong Cài đặt".
- [ ] Xoá **cả thư mục** `docs/todo/` khi không còn file nợ nào
