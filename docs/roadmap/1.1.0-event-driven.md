# 1.1.0 — Event-driven kernel (Epic D)

> Task `T9888c11b` · Parent [#188](https://github.com/naut1402/agent-workflow/issues/188) · Epic [#193](https://github.com/naut1402/agent-workflow/issues/193) · Observability [#195](https://github.com/naut1402/agent-workflow/issues/195) / [#196](https://github.com/naut1402/agent-workflow/issues/196)

## Chiến lược nhánh (bắt buộc)

Cây `dev/1.1.0/main` cũ sẽ **reset** (nhiều xử lý NG). **Không** kế thừa code từ đó.

Khi implement:

1. Branch **`dev/1.1.0/event-driven`** từ **`main` mới nhất**
2. Cherry-pick / port phần Epic D sạch từ lịch sử 1.1.0 (ưu tiên `983b1af`; `0ec06c4` lẫn E/G — chỉ lấy `emit`)
3. Đóng gap + emit CRUD theo feature; quan sát qua #195/#196

## Sub-issue (đã tạo — chưa implement trong investigate)

| Issue | Việc |
|-------|------|
| [#195](https://github.com/naut1402/agent-workflow/issues/195) | Tab **events** trong Logs (khó verify trên UI nghiệp vụ) |
| [#196](https://github.com/naut1402/agent-workflow/issues/196) | Setting `logging.types.events` bật/tắt ghi event log |

## Hướng emit

- Lifecycle: `job.*` (gồm `queued` / `cancelled` / early-fail), `task.*`, `hitl.*`, …
- CRUD domain: `entity.created|updated|deleted` qua `emitEntity` — **sau persist OK**, song song `emitAudit` (không thay audit).
- Nguyên tắc: **persist → emit**; payload tối thiểu (id + định danh; không secret).

## Checklist implement (kernel PR)

| # | Hạng mục | Status |
|---|----------|--------|
| 1 | `job.queued` / `job.cancelled` / early-fail emit | Done |
| 2 | Emit sau `writeStateAtomic` (`hitl.pending` / `task.advanced`) | Done |
| 3 | Review-retry → `task.advanced` (`reason: review_retry`) | Done |
| 4 | D3 registry stub — contract only; wire runtime TBD | Done (stub) |
| 5 | Test emit + error isolation + job lifecycle | Partial — TC-01…11 Done; còn thiếu TC-12 (cancel finished không emit), TC-13 (advance no-op), TC-14 (CRUD controller), TC-15 (credential không secret) |
| 6 | `emitEntity` + wire tối thiểu (monitor project / runner) | Done — project CRUD nằm ở `monitor` (sau migrate từ settings) |
| 7 | `cloneProject` → `entity.created` project (không `task.created`) | Done — `createProject` clone path emit `entity.created` |
| 8 | CRUD residual (pipeline-editor / agent-editor / knowledge / …) | Follow-up trên cùng branch trước merge nếu cần |
| 9 | #195 / #196 (prefs mặc định **off**) | Out of scope — PR con base `dev/1.1.0/event-driven` |

## Quyết định §7 (đã xác nhận)

| # | Quyết định |
|---|------------|
| 1 | `job.cancelled` |
| 2 | D3 wire = TBD (ngoài task) |
| 3 | `logging.types.events` mặc định **off** |
| 4 | Naming CRUD: **`entity.*`** |
| 5 | `0ec06c4`: chỉ port phần cần — không lẫn E/G |
| 6 | #195/#196 base từ **`dev/1.1.0/event-driven`** |

## Artifact

`.dev-team-agent/tasks/T9888c11b/investigate.md` / `design.md` (gitignored trên dashboard project).
