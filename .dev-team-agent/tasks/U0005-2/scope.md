# Scope — U0005-2: Quick actions trên artifact viewer

**Parent:** U0005  
**Phụ thuộc:** Không.

## Outcome

Toolbar artifact → submit job → poll → reload.

## File chính

`server/artifactActions/`, `ArtifactPanel.vue`, `useArtifactAction.ts`, `artifact-actions.yaml`

## Done criteria

- Seed `improve-doc`; job succeeded → reload artifact
