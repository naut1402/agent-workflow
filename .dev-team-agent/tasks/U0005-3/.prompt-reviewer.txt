Task: dev-agent-teams:reviewer U0005-3

Context:
- Workspace: .dev-team-agent/tasks/U0005-3/
- State: .dev-team-agent/.dev-state/U0005-3.json
- Project rules: .dev-team-agent/project-rules.md
- Pipeline step id: reviewer
- Step name: Review
- Required artifacts: review.md, test-spec.md
- Export JSON: false
- Knowledge inputs: none
- Git branch: feat/U0005-3/agent-build-wizard
- Commit cần review: d872b99 "feat(monitor): build agent từ NL qua runner (U0005-3)"

Đối tượng review = git diff của commit d872b99 (so với be73d6d) + typecheck.md.
Files trong diff:
- src/api/client.ts (buildAndRunAgent helper)
- src/features/monitor/components/AgentBuildWizard.vue (mới)
- src/features/monitor/components/MonitorLayout.vue (nút + render wizard)
- src/features/monitor/composables/useAgentBuild.ts (mới)
- tests/src/api/client.buildAndRunAgent.test.ts
- tests/src/features/monitor/composables/useAgentBuild.test.ts

Instructions:
- Apply skills: coding-rules, write-tests
- Apply project-rules section(s): coding, test (fallback coding-rules nếu trống — section có sẵn).
- Review theo coding conventions: ESM+TS, `<script setup lang="ts">`, không enum, không default export sai chỗ, Zod ở biên I/O, UI strings tiếng Việt, logic tách composable, error handling runner disabled/no-runner.
- Kiểm tra: đúng scope Feature 2 (NL→draft→save→smoke job qua runner), done criteria (generate + save + job smoke; runner disabled có message rõ), edge cases (poll timeout, transient error, empty description).
- Ghi review.md: đánh giá, danh sách issue phân loại must_fix / nice_to_have. Nếu có must_fix → nêu rõ để orchestrator quyết định retry.
- Ghi test-spec.md: test coverage hiện có + gap, spec test cần bổ sung (theo skill write-tests, mirror tests/src/**).
- Chỉ ghi 2 artifact: review.md, test-spec.md. KHÔNG sửa code nguồn ở step này.
- Nếu ambiguity chặn, ghi qa.md rồi dừng.

Khi xong trả về: verdict tổng (APPROVED / MUST_FIX), số issue must_fix, tóm tắt ngắn.
