Task: dev-agent-teams:implementer U0005-3

Context:
- Workspace: .dev-team-agent/tasks/U0005-3/
- State: .dev-team-agent/.dev-state/U0005-3.json
- Project rules: .dev-team-agent/project-rules.md
- Pipeline step id: implementer
- Step name: Implement
- Required artifacts: typecheck.md
- Export JSON: false
- Knowledge inputs: none
- Git branch (ĐÃ tạo sẵn, làm việc trên branch này): feat/U0005-3/agent-build-wizard
  (base = origin/feat/U0005/dashboard-agent-integration, đã có U0005-1 + U0005-2 merged)

Scope (U0005-3 = Feature 2 trong design.md §4.3): "Build agent từ NL qua runner".
Đọc design.md §4.3 kỹ. Đây là feature FRONTEND — backend + API client helper đã có sẵn.

Flow người dùng cần dựng:
1. Nút "Build agent" trên monitor toolbar (MonitorLayout.vue) mở wizard.
2. Wizard 3 bước:
   - Mô tả (textarea NL) → generateAgentDraft(description)  [POST /api/custom-agents/generate]
   - Preview draft (name, description, skills, sections) — cho phép chỉnh sửa inline (ít nhất name/description).
   - Lưu & chạy thử: saveCustomAgent(draft)  [POST /api/custom-agents] → submitJob(payload) với agentRef = `custom:<name>`, userPrompt smoke, workspace phù hợp (tasks/<task-id>/ nếu có task context; nếu không, custom-agents sandbox).
3. Chọn runner từ dropdown: fetchRunners().
4. Panel job status: poll fetchJob(id) tới khi done/failed. Runner disabled / không có runner → hiển thị message tiếng Việt rõ ràng (KHÔNG crash).

Helper client (src/api/client.ts) ĐÃ CÓ: generateAgentDraft, saveCustomAgent, fetchRunners, submitJob, fetchJob.
- Bọc thêm helper `buildAndRunAgent()` trong src/api/client.ts (compose save + submitJob) như design §4.3 gợi ý, có test đơn vị.

File dự kiến (theo design §4.3):
- src/features/monitor/components/AgentBuildWizard.vue (mới)
- src/features/monitor/components/MonitorLayout.vue (thêm nút mở wizard + render wizard)
- src/api/client.ts (thêm buildAndRunAgent helper)
- Tách logic suy diễn (bước wizard, poll job) ra composable nếu hợp lý: src/features/monitor/composables/useAgentBuild.ts

Instructions:
- Apply skills: coding-rules, verify-dashboard
- Apply project-rules section(s): coding (fallback skill coding-rules nếu section trống — section có sẵn).
- Tuân thủ: ESM + TS, `<script setup lang="ts">`, không enum, không default export trừ khi framework yêu cầu, Zod cho biên I/O, UI strings tiếng Việt, logic tách composable/lib.
- Test-first cho logic mới (useAgentBuild + buildAndRunAgent): vitest mirror tests/src/**. Component test cho wizard nếu khả thi.
- Chạy typecheck + test suite (skill verify-dashboard). Ghi kết quả đầy đủ (lệnh, pass/fail, số test) vào .dev-team-agent/tasks/U0005-3/typecheck.md.
- Git hygiene: stage chọn lọc (KHÔNG git add -A mù), git mv khi rename. Chỉ commit code nguồn + test liên quan feature này. KHÔNG commit .dev-team-agent/ (orchestrator lo phần đó). Commit với conventional prefix + trailer Co-Authored-By.
- Chỉ ghi artifact khai báo cho step này (typecheck.md) trong workspace; code vào src/.
- Nếu bị block bởi ambiguity, ghi qa.md theo format Q&A và dừng.
