// Namespace skeleton — filled by the `pipelineEditor` module i18n slice (see epic #94).
// Keep the export; slices add keys here + matching keys in en/pipelineEditor.ts.
export const pipelineEditor = {}
